# Database connection (SHARED with OnePieceHaus)
# Use the same PostgreSQL database for both apps
DATABASE_URL="postgresql://user:password@localhost:5432/animehaus?schema=public"

# Environment
NODE_ENV="development"

# Optional: Force DB mode even in development
# FORCE_DB=true

# Optional: Custom data directory for file-based storage (dev only)
# DATA_DIR="./.data"
